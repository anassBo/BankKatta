import { OnlyNumberValidatorDirective } from './only-number.directive';

describe('OnlyNumberValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
